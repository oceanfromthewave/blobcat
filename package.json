{
    "name": "cat-pet-statusbar",
    "displayName": "BlobCat - Status Bar Pet 🐱",
    "description": "Cute animated blobcat pet in your VS Code status bar",
    "version": "2.0.6",
    "publisher": "blobcat",

    "icon": "media/icon.png",

    "engines": {
        "vscode": "^1.90.0"
    },

    "categories": [
        "Other"
    ],

    "keywords": [
        "blobcat",
        "cat",
        "pet",
        "statusbar",
        "cute",
        "animation",
        "vscode",
        "mascot",
        "fun"
    ],

    "activationEvents": [
        "onStartupFinished"
    ],

    "main": "./out/extension.js",

    "contributes": {
        "commands": [
            {
                "command": "cat-pet.install",
                "title": "Cat Pet: Install Status Bar Pet"
            },
            {
                "command": "cat-pet.uninstall",
                "title": "Cat Pet: Remove Status Bar Pet"
            }
        ],
        "configuration": {
            "title": "BlobCat",
            "properties": {
                "blobcat.enableSleep": {
                    "type": "boolean",
                    "default": true,
                    "description": "Close the cat's eyes after a period of inactivity."
                },
                "blobcat.enableParticles": {
                    "type": "boolean",
                    "default": true,
                    "description": "Spawn pink particles when the cat jumps on keystroke."
                },
                "blobcat.catScale": {
                    "type": "number",
                    "default": 1,
                    "minimum": 0.5,
                    "maximum": 3,
                    "description": "Size multiplier for the cat (1 = default 26px)."
                },
                "blobcat.catOpacity": {
                    "type": "number",
                    "default": 1,
                    "minimum": 0.1,
                    "maximum": 1,
                    "description": "Opacity of the cat (0.1 – 1)."
                },
                "blobcat.catColor": {
                    "type": "string",
                    "default": "#FFFFFF",
                    "description": "Body color of the cat (CSS color string, e.g. '#FFFFFF', 'orange', '#FFB6A3')."
                },
                "blobcat.enablePetting": {
                    "type": "boolean",
                    "default": true,
                    "description": "Click the cat to pet it — triggers a happy wiggle and spawns hearts."
                }
            }
        }
    },

    "scripts": {
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "clean": "rimraf out",
        "rebuild": "npm run clean && npm run compile",
        "vscode:prepublish": "npm run compile"
    },

    "devDependencies": {
        "@types/node": "^20.12.12",
        "@types/vscode": "^1.90.0",
        "typescript": "^5.4.5",
        "rimraf": "^5.0.5"
    },

    "repository": {
        "type": "git",
        "url": "https://github.com/oceanfromthewave/blobcat"
    },

    "license": "MIT"
}